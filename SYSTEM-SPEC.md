# ResolveDesk 系统行为规格

> 更新时间：2026-08-21
> 文档状态：全栈一期开发前基线
> 当前需求：`changes/active/ai-customer-support-platform/`
> 规格依据：`changes/active/ai-customer-support-platform/spec.md`

## 1. 文档目的

本文档描述 ResolveDesk 在当前代码基线中已经具备的行为，以及已批准但尚未实现的全栈一期目标。它用于帮助后续设计、编码、审查和验收准确区分“当前系统”与“目标系统”，不得把目标能力描述为已经上线。

状态标记：

- **已实现**：当前代码中存在并可由现有测试或配置验证。
- **待替换**：当前模板能力将在全栈一期退出产品范围。
- **已批准、待实现**：已由 `proposal.md` 和 `spec.md` 确认，但尚未编码。
- **后续阶段**：不属于本期开发范围。

## 2. 产品定位

ResolveDesk 的目标是单工作空间客服工单平台。客户通过工单提交问题，人工客服接手并处理，管理员负责用户、角色和全局工单管理。

全栈一期只建立稳定的客服业务闭环，不接入 AI。全栈一期完成后，再单独立项加入服务于人工客服的 AI Copilot。

## 3. 当前系统基线

### 3.1 技术与运行能力

| 能力 | 当前状态 | 系统行为 |
|---|---|---|
| 后端 API | 已实现 | FastAPI 提供版本前缀为 `/api/v1` 的 HTTP API，并暴露 OpenAPI 文档 |
| 数据库 | 已实现 | 使用 PostgreSQL 和 SQLModel，结构变更由 Alembic 管理 |
| 前端 | 已实现 | React、TypeScript 和 Vite 构建单页应用，使用 TanStack Router 和 TanStack Query |
| 前端客户端 | 已实现 | 根据后端 OpenAPI 生成 TypeScript 客户端 |
| 鉴权 | 已实现 | 用户使用邮箱和密码登录，成功后使用 Bearer JWT 访问受保护接口 |
| 密码安全 | 已实现 | 密码以安全哈希保存，支持登录时校验和哈希升级 |
| 邮件 | 已实现 | 支持新账号邮件、测试邮件和密码重置邮件；是否启用由 SMTP 配置决定 |
| 监控 | 已实现 | 非开发环境可配置 Sentry |
| 跨域 | 已实现 | 后端只允许配置的前端地址跨域访问 |
| 容器运行 | 已实现 | Docker Compose 提供 PostgreSQL、FastAPI、Traefik 和 Adminer |
| 自动化测试 | 已实现 | 后端使用 Pytest，前端关键流程使用 Playwright，GitHub Actions 执行相关检查 |

### 3.2 当前用户行为

| 行为 | 当前状态 | 说明 |
|---|---|---|
| 公开注册 | 已实现 | 访客可以使用邮箱、密码和姓名注册普通用户 |
| 用户登录 | 已实现 | 活跃用户使用邮箱和密码获取访问令牌 |
| 当前用户资料 | 已实现 | 登录用户可以读取并修改自己的邮箱和姓名 |
| 修改密码 | 已实现 | 用户提交正确当前密码后可以设置不同的新密码 |
| 删除自己 | 已实现 | 普通用户可以删除自己；超级管理员不能删除自己 |
| 密码找回 | 已实现 | 用户可以通过邮件令牌重置密码 |
| 用户管理 | 已实现 | 超级管理员可以查询、创建、修改和删除其他用户 |
| 用户状态 | 已实现 | 用户具有启用状态；停用用户不能正常访问系统 |
| 权限模型 | 待替换 | 当前只有普通用户和 `is_superuser` 超级管理员两级权限 |

### 3.3 当前 Item 示例业务

| 行为 | 当前状态 | 说明 |
|---|---|---|
| 创建 Item | 待替换 | 登录用户可以创建归属于自己的 Item |
| Item 列表 | 待替换 | 普通用户只看到自己的 Item，超级管理员看到全部 Item |
| Item 详情和编辑 | 待替换 | Owner 或超级管理员可以查看和编辑 |
| 删除 Item | 待替换 | Owner 或超级管理员可以删除 |
| 前端 Items 页面 | 待替换 | 提供 Item 表格及新增、编辑、删除交互 |
| Item 自动化测试 | 待替换 | 后端和 Playwright 已覆盖 Item CRUD |

Item 是模板示例资源，不属于 ResolveDesk 最终产品。全栈一期不会把历史 Item 转换为工单，新版本将退出 Item API、页面、导航和对应测试。

## 4. 已批准的全栈一期目标

> 本章全部标记为“已批准、待实现”。详细验收依据以 `changes/active/ai-customer-support-platform/spec.md` 为准。

### 4.1 工作空间边界

- 系统只有一个逻辑工作空间。
- 所有用户都属于该系统，不创建 Workspace 实体，不提供租户选择或切换。
- 不实现跨租户隔离、组织邀请、团队或客服组长。

### 4.2 用户角色

每个用户必须且只能拥有一个角色：

| 角色 | 定义 | 核心职责 |
|---|---|---|
| `CUSTOMER` | 客户 | 创建、查看和回复自己的工单 |
| `AGENT` | 人工客服 | 查看公共队列，接手并处理分派给自己的工单 |
| `ADMIN` | 管理员 | 管理用户与角色，查看、分派和管理全部工单 |

约束：

- 公开注册用户固定成为 Customer。
- 只有 Admin 可以创建非 Customer 用户或修改其他用户角色。
- Agent 是人工客服，不是 AI Agent。
- 系统必须始终保留至少一个活跃 Admin。
- 停用用户不能登录或继续访问业务资源。
- 停用 Agent 不得接手或被分派新工单。

### 4.3 用户权限

#### Customer

Customer 可以：

- 查看和修改自己的基本资料与密码；
- 创建工单；
- 查看、筛选和搜索自己的工单；
- 查看自己工单的公开时间线；
- 对自己未关闭的工单发送公开回复；
- 查看自己的基础工单统计。

Customer 不可以：

- 查看其他客户的工单；
- 查看任何内部备注；
- 接手、分派、转派或删除工单；
- 修改工单优先级和分类；
- 访问 Agent 工作队列或 Admin 用户管理。

#### Agent

Agent 可以：

- 查看所有未分派工单；
- 查看分派给自己的工单；
- 接手未分派且未关闭的工单；
- 查看未分派或自己负责工单的详情；
- 对自己负责的未关闭工单发送公开回复；
- 对自己负责的未关闭工单添加内部备注；
- 修改自己负责工单的状态、优先级和分类；
- 查看本人工作队列的基础统计。

Agent 不可以：

- 查看其他 Agent 已负责的工单；
- 把工单转派给其他 Agent；
- 取消工单分派；
- 管理用户、角色或启用状态；
- 删除工单。

#### Admin

Admin 可以：

- 查看、筛选和搜索全部工单；
- 查看全部工单详情、公开回复、内部备注和操作记录；
- 接手、分派、转派或取消分派未关闭工单；
- 对未关闭工单发送公开回复和内部备注；
- 按状态机修改工单状态；
- 修改未关闭工单的优先级和分类；
- 创建用户、修改其他用户角色和启用状态；
- 查看全局基础统计；
- 确认后删除工单。

Admin 不可以：

- 让系统失去最后一个活跃 Admin；
- 修改 `CLOSED` 工单的业务内容；
- 将工单分派给 Customer、停用用户或不存在的用户。

### 4.4 工单数据行为

每个工单至少包含：

- 唯一且不可修改的工单编号；
- 标题；
- 问题描述；
- 状态；
- 优先级；
- 分类；
- 创建人；
- 可为空的负责人；
- 创建时间与更新时间。

固定枚举：

- 状态：`OPEN`、`IN_PROGRESS`、`WAITING_FOR_CUSTOMER`、`RESOLVED`、`CLOSED`。
- 优先级：`LOW`、`MEDIUM`、`HIGH`、`URGENT`。
- 分类：`ACCOUNT`、`BILLING`、`PRODUCT`、`BUG`、`FEATURE_REQUEST`、`OTHER`。

新工单默认行为：

- 状态为 `OPEN`；
- 优先级为 `MEDIUM`；
- 负责人为空；
- 创建人是当前 Customer；
- 列表默认按最近更新时间倒序。

字段约束：

- 标题去除首尾空白后为 1～200 个字符；
- 描述去除首尾空白后为 1～10,000 个字符；
- 回复和备注去除首尾空白后为 1～10,000 个字符；
- 时间统一以 UTC 保存并返回带时区值。

### 4.5 工单状态机

允许的状态变化：

```text
OPEN
  └─ Agent 接手或 Admin 分派 → IN_PROGRESS

IN_PROGRESS
  ├─ 负责 Agent/Admin 请求客户补充 → WAITING_FOR_CUSTOMER
  └─ 负责 Agent/Admin 确认解决 → RESOLVED

WAITING_FOR_CUSTOMER
  ├─ Customer 公开回复 → IN_PROGRESS
  ├─ 负责 Agent/Admin 恢复处理 → IN_PROGRESS
  └─ 负责 Agent/Admin 确认解决 → RESOLVED

RESOLVED
  ├─ Customer 公开回复 → IN_PROGRESS
  ├─ Admin 重新打开 → IN_PROGRESS
  └─ Admin 确认结束 → CLOSED

CLOSED
  └─ 不允许继续变更
```

补充规则：

- Agent 接手或 Admin 分派 `OPEN` 工单后，工单自动进入 `IN_PROGRESS`。
- Admin 取消未关闭工单分派后，负责人清空，状态变为 `OPEN`。
- Customer 回复 `WAITING_FOR_CUSTOMER` 或 `RESOLVED` 工单后，状态自动进入 `IN_PROGRESS`，原负责人保持不变。
- `CLOSED` 工单只读，任何角色都不能继续回复或修改负责人、状态、优先级和分类。
- 不符合状态机的请求必须被拒绝，且不能部分更新数据。

### 4.6 接手与分派

- 活跃 Agent 可以接手 `OPEN`、未分派、未关闭工单。
- 多个 Agent 同时接手同一工单时只能有一个成功，后提交者不能覆盖负责人。
- Admin 可以把未关闭工单分派或转派给活跃 Agent。
- Agent 不能主动转派给其他 Agent。
- 工单转派后，原 Agent 立即失去该工单详情和操作权限。
- 分派、转派和取消分派必须写入操作记录。

### 4.7 消息与时间线

工单消息分为：

- `PUBLIC_REPLY`：Customer、可访问该工单的 Agent 和 Admin 可见。
- `INTERNAL_NOTE`：当前负责人 Agent 和 Admin 可见；未分派工单中的内部备注不向浏览公共队列的 Agent 返回。

行为约束：

- Customer 只能发送公开回复。
- Agent 只能对自己负责的未关闭工单发送回复或内部备注。
- Admin 可以对任意未关闭工单发送回复或内部备注。
- 时间线按消息创建时间正序展示。
- Customer 的任何接口响应都不能包含内部备注正文、类型、数量、占位信息或可推断内部备注存在的信息。
- Agent 浏览未分派工单时，响应同样不能包含内部备注正文、类型、数量、占位信息或可推断其存在的信息。
- 停用用户的历史消息保留，并继续显示其历史身份信息。

### 4.8 工单操作记录

以下成功操作必须生成记录：

- Agent 接手；
- Admin 分派、转派或取消分派；
- 状态变化；
- 优先级变化；
- 分类变化；
- Admin 删除工单。

每条记录至少表达：

- 工单标识；
- 操作者；
- 操作类型；
- 变更前值与变更后值；
- 操作时间。

记录行为：

- 普通业务用户不能修改或删除记录。
- Customer 只能看到自己工单中允许公开的状态和业务变化。
- Customer 不得看到内部备注事件或敏感客服管理说明。
- 工单删除后，删除审计仍需保留。

### 4.9 查询、分页与统计

工单列表：

- 默认每页 20 条；
- 每页最少 1 条，最多 100 条；
- 响应返回当前数据和总数；
- 支持状态、优先级、分类和负责人筛选；
- 关键字只匹配工单编号和标题；
- 所有查询先应用当前角色的数据权限，再应用筛选和分页。

角色统计：

- Customer：自己的各状态工单数量；
- Agent：未分派、分派给自己、等待客户的工单数量；
- Admin：全局各状态、各优先级和未分派工单数量。

已删除工单不进入列表和统计。

### 4.10 用户生命周期

- 历史工单、消息或操作记录关联的用户不进行物理删除，通过停用终止访问。
- 用户停用不删除其历史业务记录。
- 唯一活跃 Admin 不能停用自己、删除自己或把自己降级为其他角色。
- 现有模板超级管理员在迁移后成为 `ADMIN`。
- 现有模板普通用户在迁移后成为 `CUSTOMER`。

### 4.11 前端行为

Customer 登录后主要看到：

- 个人统计首页；
- “我的工单”列表；
- 创建工单入口；
- 工单详情及公开回复；
- 个人设置。

Agent 登录后主要看到：

- 本人工作统计首页；
- 未分派公共队列；
- 分派给自己的工单；
- 接手、回复、内部备注、状态和属性操作；
- 个人设置。

Admin 登录后主要看到：

- 全局统计首页；
- 全部工单管理；
- 分派、转派、关闭和删除操作；
- 用户、角色和启用状态管理；
- 个人设置。

前端规则：

- 无权功能不显示在导航和操作区。
- 直接输入无权路由时不得显示受保护数据。
- 前端隐藏不能代替后端权限校验。
- 工单和用户接口统一使用 OpenAPI 生成客户端，不维护第二套重复请求定义。
- 加载、空结果、字段校验、权限拒绝、资源不存在和并发冲突均需提供明确状态反馈。

## 5. 迁移与兼容行为

全栈一期迁移必须满足：

1. 现有用户账号、密码哈希和个人资料继续有效。
2. `is_superuser=true` 的现有用户映射为 `ADMIN`。
3. 其他现有用户映射为 `CUSTOMER`。
4. 迁移后至少存在一个活跃 Admin，否则迁移不得被视为成功。
5. Item 示例数据不转换为 Ticket。
6. 新产品不再展示 Items 导航和页面，也不再提供 Item 业务入口。
7. 登录、注册、密码重置、个人设置、邮件配置和 Docker 启动行为继续可用。

## 6. 测试与验收基线

### 后端必须覆盖

- 公开注册默认角色；
- 三角色接口和数据范围；
- 停用用户访问拒绝；
- 唯一活跃 Admin 保护；
- 工单创建、筛选、分页和搜索；
- Customer 跨用户访问拒绝；
- Agent 跨负责人访问拒绝；
- 并发接手只能一个成功；
- Admin 分派、转派和取消分派；
- 公开回复和内部备注隔离；
- 完整状态机与非法转换；
- `CLOSED` 工单只读；
- 操作记录裁剪和删除审计；
- 角色化统计的数据范围；
- 用户角色迁移和 Item 退出后的回归行为；
- OpenAPI 契约。

### 前端 E2E 必须覆盖

- Customer 注册、登录、创建工单、查看和回复自己的工单；
- Customer 无法进入 Agent 和 Admin 功能；
- Agent 查看公共队列、接手、公开回复、内部备注、等待客户和解决；
- Agent 无法管理用户或操作其他 Agent 工单；
- Admin 创建 Agent、分派、转派、关闭工单和管理用户状态；
- Customer 回复等待或已解决工单后自动重新进入处理；
- 关闭工单只读；
- 三类角色对应导航和统计；
- 登录、密码重置和个人设置回归。

### 运行验收

- 后端测试通过；
- 后端 lint 和类型检查通过；
- 前端 TypeScript 构建和代码检查通过；
- Playwright 关键流程通过；
- Docker Compose 启动和健康检查通过；
- OpenAPI 生成客户端与调用方类型一致。

## 7. 明确排除范围

全栈一期不包含：

- 多租户 Workspace；
- 团队、客服组长和团队队列；
- AI Copilot；
- RAG、Embedding、知识库和 Tool Calling；
- 实时聊天和 WebSocket；
- 邮件收件转工单；
- SLA 和自动升级；
- 文件附件；
- 标签和自定义字段；
- 满意度评价；
- 批量操作、导入和导出；
- 复杂报表。

## 8. 后续阶段

全栈一期通过审查和验收后，AI Copilot 必须作为独立需求重新经过 proposal、spec 和 design 流程。预期方向包括工单摘要、分类建议、回复建议、流式输出和 AI 调用记录，但这些内容当前没有形成已批准功能，不得提前进入本期代码。

## 9. 开发顺序

开发门控顺序固定为：

```text
后端领域模型与迁移
    ↓
后端角色权限与业务接口
    ↓
后端自动化测试和 OpenAPI 契约通过
    ↓
生成前端客户端
    ↓
前端角色化页面和操作
    ↓
前端 E2E 测试
    ↓
三角色全栈联调
    ↓
认证、部署和模板能力回归
```

在后端业务接口和契约未稳定前，不开始对应前端业务页面开发。
